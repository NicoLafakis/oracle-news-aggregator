# Oracle News Aggregator Dependencies

# HTTP requests
requests>=2.31.0

# RSS feed parsing
feedparser>=6.0.10

# Date/time handling (standard library, but useful extensions)
python-dateutil>=2.8.2

# Optional: Better text similarity (uncomment if you want advanced dedup)
# rapidfuzz>=3.0.0
